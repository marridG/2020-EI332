
测试题 1
在你自己设计完成的单周期和流水线CPU基础上，修改或者添加一个地址为A8h的32位输出端口，端口名字定义为你的姓名的全拼。
以如下两段mif文件作为指令和数据存储器初始值，完成对单周期和流水线CPU的仿真。注意存储器大小与你的设计匹配。
其中，sc_instmem.mif为指令存储器初始值，不可做修改。
sc_datamen.mif为数据存储器初始值，必须修改第三个单元的数据，将你的学号后8位直接作为16进制数据存入该单元。（给你的文件里这个值是30910256h，代表学号是518030910256，请自行修改。）
自行编写仿真激励波形文件，要求如下。CPU指令周期设置为10ns，CPU复位信号初值为复位有效状态，30ns后无效。


测试题2
为流水线CPU自行添加一个新指令cont，指令类型格式和对应机器码自行确定。
将你的学号后8位作为一个16进制数据放在数据存储器0地址，计算这个数的汉明重量（32bit中含1的bit的数量），并通过A8h端口输出。设计测试代码完成仿真。
测试代码要完成如下循环动作： 从数据内存地址00处读入待处理数据学号的后8位到R4，例如，假设学号后8位是30910001，对16进制数据30910001h按照新指令执行操作，将结果写入R6寄存器。则R6和指定输出端口输出应为00000006h。之后再次循环。
自行编写仿真激励波形文件，要求如下。CPU指令周期设置为20ns，CPU复位信号初值为复位有效状态，30ns后无效。
